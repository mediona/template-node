{
  "name": "my-node-server",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "build": "copyfiles -u 1 src/* dist",
    "start": "node dist/server.js",
    "test": "jest"
  },
  "dependencies": {
    "copyfiles": "^2.4.1"
  },
  "devDependencies": {
    "jest": "^29.x.x"
  }
}
